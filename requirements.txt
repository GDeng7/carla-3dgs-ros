numpy>=1.24
PyYAML>=6.0
pygame>=2.5; platform_system == "Linux"
scipy>=1.10; platform_system == "Linux"
grpcio>=1.60; platform_system == "Linux"

# ROS2 bridge (install matching your ROS2 distro on Ubuntu)
# rclpy is provided by ROS2 — do not pip install on Linux with ROS2 sourced.
# For development on macOS without ROS2, these are optional:
# rclpy
# sensor-msgs
# nav-msgs
# geometry-msgs
# rosgraph-msgs
# builtin-interfaces